# Movera Admin (A1–A2 scaffold)

React + TypeScript + Vite. Layout shell, URL routing, and design tokens extracted
from the static `styles.css`.

The live GitHub Pages site is still the three static files at repo root until A3
ports the 22 screens into this app.

```bash
npm install
npm run dev
```
