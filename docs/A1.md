# A1 — Admin framework

**Decision (21 September 2026): React + TypeScript + Vite.**

Typed contracts from P3 codegen to TypeScript. The current three static files
(`index.html`, `app.js`, `styles.css`) stay the live GitHub Pages site until
a later cutover. A3 ported all 22 admin screens into `webapp/` with typed mocks
and the static visual system copied into `webapp/src/styles/tokens.css`.

This scaffold does not replace the Grok Driver preview.
